import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function LoginPage() {
  return (
    <PagePlaceholder
      title="Login"
      description="Staff and owner sign-in will live here, backed by Supabase Auth. Not wired up yet."
    />
  );
}
